import initialUserState from "./states/userState";
import initialCounterState from "./states/counterState";

const initialState = {
	user: initialUserState,
	counter: initialCounterState
}

export default initialState;
