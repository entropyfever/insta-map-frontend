import cardsState from "./cards/state";
import popupsState from "./popups/state";
import auxState from "./aux/state";

const initialState = {
	CARDS: cardsState,
	POPUPS: popupsState,
	AUX: auxState,
}

export default initialState;
